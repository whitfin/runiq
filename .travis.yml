# Language flags.
language: rust

# OS matrix.
os:
  - linux
  - osx
  - windows

# Version matrix.
rust:
  - stable
  - beta
  - nightly

# Matrix modifications.
matrix:

  # Failures to allow.
  allow_failures:
    - rust: nightly

# Cargo package caching.
cache:
  cargo: true
